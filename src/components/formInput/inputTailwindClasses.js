const borderClasses = 'rounded-full border-0';
const placeholderClasses = 'placeholder:text-xl placeholder:text-gray-100 font-semibold';
const metricsClasses = 'w-3/4 py-4 pl-10';
const focusClasses = 'focus:outline-0 focus:ring-2 focus:ring-gray-300 focus-visible:outline-0'
const textClasses = 'text-gray-300 text-xl';
const misc = 'bg-white duration-300';

const inputTailwindClasses = `${textClasses} ${borderClasses} ${placeholderClasses} ${metricsClasses} ${focusClasses} ${misc}`

export default inputTailwindClasses;
